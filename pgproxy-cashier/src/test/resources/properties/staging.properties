#Setting the fixed properties as Jedis. Used by common/cache

redisUrl=redis://localhost:6379/0#mymaster

redis.client.implementation=JEDIS

redis.max.total=500
redis.max.idle=100
redis.min.idle=10
redis.max.wait=2000
